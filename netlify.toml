[build]
  command = "npm run build"
  publish = "dist"

# Redirige todas las rutas al index correcto (necesario para /admin/)
[[redirects]]
  from = "/admin/*"
  to   = "/admin/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200